# cache / scratch
_scratch/
workspace-scratch/
.pytest_cache/
__pycache__/
*.pyc

# AppData migration (not part of Astral)
AOS/
backup-appdata/
tmp_compat.py

# temp landing files
.hermes-tmp.*

# node / build
node_modules/
.next/
.vercel/
dist/

# large binary
*.png
*.jpg
*.jpeg
*.gif
*.ico
*.mp4
*.webp

# OS
.DS_Store
Thumbs.db

# local-only SQLite cache (untracked by design; rebuilt from db/migrations + db/seeds)
backend/astral.db
backend/db/backups/

# nested clone of this very repo (was a gitlink with no .gitmodules, which made
# `git clone --recurse-submodules` fail and a fresh clone impossible to repair).
# git history already backs the vault up; keep the directory local-only.
obsidian-vault/backups/obsidian-vault-backup/

# secrets / config
.env
render.yaml

# HF model cache (13GB local)
.cache/

# Hermes Agent (install, data, secrets, backups)
hermes/
hermes-backup-*/
hermes-reinstall.log
hermes-desktop-reinstall.log
