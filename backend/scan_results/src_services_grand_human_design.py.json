{"path": "C:\\AI\\backend\\src\\services\\grand\\human_design.py", "analysis": "BUGS: `lon_to_gate_line` uses `min(line, 6)` but the wheel has 6 lines (1-6), so it's redundant. `_load_wheel()` returns a dict with \"order\", but `activated_gates` expects a list.\n\nDEAD: Unused import `from src.services.chart_service import BODIES` in `_body_longitudes_utc`.\n\nRISK: `_sun_longitude_skyfield` and `_body_longitudes_utc` use skyfield which is J2000-referenced, potentially causing gate/line errors for historical charts. No error handling for missing data files.\n\nSMELL: `classify_type` and `inner_authority` have complex logic; could be refactored into separate functions or rules. `_load_wheel()` and `_sun_longitude_utc` are duplicated in multiple places.\n\nOK: CLEAN — The code is logically consistent, handles edge cases, and follows the Human Design specification with clear structure."}